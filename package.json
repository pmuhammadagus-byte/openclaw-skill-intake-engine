{
  "name": "@pmuhammadagus-byte/openclaw-skill-intake-engine",
  "version": "0.1.0",
  "description": "Secure intake, analysis, validation and registry tooling for OpenClaw skills.",
  "type": "module",
  "files": ["dist", "openclaw.plugin.json", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "plugin:build": "npm run build && openclaw plugins build --entry ./dist/index.js",
    "plugin:validate": "npm run build && openclaw plugins validate --entry ./dist/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "typebox": "^1.1.39"
  },
  "peerDependencies": {
    "openclaw": ">=2026.5.17"
  },
  "openclaw": {
    "extensions": ["./dist/index.js"],
    "compat": {
      "pluginApi": ">=2026.5.17"
    }
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
